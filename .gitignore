node_modules/
scratch/
*.log
.DS_Store

# secrets — never commit your API key
.env
.env.local

# assets/ is TRACKED as of U7: store builds clone the repo and hand-provided
# sprites are not regenerable, so untracked art would silently ship blank.
# Only generation debris stays ignored (top-level *.log already covers logs).
assets/**/*.log
