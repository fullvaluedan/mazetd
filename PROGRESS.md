# Progress

One line per phase as it's completed.

- **Phase 0 — Scaffold:** ✅ Project structure, index.html (canvas + HUD shell), fixed-timestep loop (pause + 1×/2×/3×), full `config.js` data tables, seeded RNG, start scripts, docs. Dark canvas renders, loop logs ~60Hz.
- **Phase 1 — Map & pathfinding:** ✅ Grid + cell types, seeded obstacle scatter (6–10 clusters) with reachability guarantee (regenerates if a spawn is trapped), 3 spawns / 2 goals with markers + labels, BFS distance fields + A* + path tracing, animated dotted path overlay (toggle `P`), and the Wintermaul build-legality check. Verified across 40 seeds: all stay connected; greedy interior fill (322 towers) never fully blocks a route.
- **Phase 2 — Enemies move & leak:** ✅ Enemy entities follow the distance-field gradient (ground) or fly straight (flying), with status-effect + damage scaffolding (armor/magic/poison/shield/shatter). Leaks cost lives, kills award gold; wave runtime (staggered spawn queue + scaling formulas) + minimal HUD (gold/lives/wave, pause/speed, Start Wave). Enemy/floater/flash rendering. Verified headless: Grunts traverse & leak (lives 20→10), ground enemy completes a 32-cell route, flyer leaks straight, no-defense run loses at wave 2.
- **Phase 3 — Towers & combat:** ✅ Build (legal cells only, re-arms for rapid placement) / select / sell (70% refund) with live maze re-routing on every build & sell. Full type-aware combat pipeline: targeting (range + anti-air gate + first/last/strongest/closest modes), homing projectiles, hitscan beams, splash, chain lightning, slow/poison/shatter/disrupt/contagion riders. HUD tower shop + selected-tower card (stats, target mode, upgrade/fork, sell). Verified headless: reroute, kills→gold, magic-vs-armor, shield-vs-magic, anti-air gating, DoT, tesla 3-chain, cannon splash.
- **Phase 4 — All towers + upgrades:** ✅ All 6 towers, L1→L4 upgrade tree with the permanent L3→L4 specialization fork (two branches each), cost scaling (1.0/1.8/3.2× base), invested-tracking for refunds, target-mode cycling, and tower visuals (level pips + branch letter). 39/39 headless checks pass: stat progression, all 12 branch specials (stats + in-action: shatter +50%, disrupt shield-strip/dispel, contagion spread, storm 6-chain no-falloff), targeting modes, and anti-air across all towers.
- **Phase 5 — Waves 1–100 + economy:** ✅ Procedural composition with the full type-introduction schedule, 10 boss waves with escalating ability kits (heal/swarmlings/speed-burst/slow-immunity), wave-50 flying boss & wave-100 double-boss, swarm packs, wave-40+ multi-type/multi-direction routing. Next-wave preview (glyphs), one-wave-ahead flying warning, early-start bonus + build timer, auto-start, wave-clear bonus + capped interest, win/lose + end modal, boss health bars. All schedule/boss/economy/win-trigger checks pass; engine stable through bosses & flyers (max build reaches ~w37 — full win-balance tuning is Phase 8).
- **Phase 6 — Heroes:** ✅ Start-screen hero selection (all 3, Ranger default), command-move with A* pathing (re-paths on maze change), auto-attack (ranged projectile / melee, anti-air per hero), contact-damage down + timed respawn at base, XP→level-10 (raises HP/damage, fully heals), and both abilities per hero on cooldowns (Whirlwind/Taunt, Meteor/Frost-Nova, Volley/Hawk-Eye) with click-to-target reticles. HUD hero panel (level/XP/HP + ability cooldown overlays). 24/24 headless checks pass.
- **Phase 7 — Shop & full HUD polish:** ✅ Hero stat upgrades (escalating tiers: +HP/+dmg/−ability-CD/−respawn, between waves) and four consumables (Repair, Frenzy +50% tower dmg, Flash Freeze, targeted Airstrike). Complete side panel + cursor-following tooltips for towers/enemies/build-preview, floating numbers, boss bars, banners/toasts, end modal + restart. 22 shop checks pass; a DOM-shim smoke test renders the HUD + a full frame across 9 game states with no runtime errors.
- **Post-launch — Art, mobile & store pass:** ✅ 19 Frozen-Throne-style assets generated via the OpenAI image tool (towers/enemies/heroes/background/app icon) + sprite layer with shape fallback and an Art toggle (sprites downscaled to 128px at load for mobile perf). Mobile: tap-hero/Move-button movement, stacked portrait layout, 40px+ touch targets, `touch-action` canvas, PWA manifest + icon, first-run hint flow. Store: permanent global Tower Boost tiers (+dmg/+atk-speed/+range), saved & balance-checked. All verified in a live browser session (start screen, battle, tap-to-move) + full headless suite. See `MOBILE_REVIEW.md` for the audit.
- **Post-launch — Vertical maze-defense campaign (C1–C6):** ✅ Dynamic per-level grids + 20 authored portrait levels with Gem-TD checkpoint chains (stage-aware routing/siege/flyers) + tall Endless map; 5g Wall block + halved tower costs (maze-first Wintermaul economy, classic gate green at unchanged damage scale via late wall→tower conversion); per-enemy atk drives wall/hero damage; hero guard-post auto-engage (chewers first, leash, drift home) with visible swings; profile (stars, persistent hero level/XP, star-bought upgrades), 21-node level-select map, tower unlock schedule, reload-per-level flow; portrait-first PWA + rotate guard flip + cream/wood UI restyle; campaign gates: reference clears all 20 (`campaign-sim.mjs`), careless no-maze play first loses at level 9 (`--careless`).
- **Post-launch — Kingdom Rush UI + monetization pass:** ✅ Fullscreen DPR-sharp letterboxed scene (viewport.js; fixed the latent input-scaling bug); all chrome over the canvas: top bar (chips, speed, pause, store, settings, FREE GOLD ad button), NEXT-WAVE button + spawn chevrons, radial build/tower rings with seal warnings, hero dock with HP arc + cooldown sweeps + KR persistent selection, settings/store sheets (auto-pause), docked info card replacing tooltips, hints v2, title/hero-select/victory/defeat screens with star ratings, rotate-to-landscape guard. Procedural WebAudio SFX behind a plain-data state.events queue. Rewarded ads (simulated provider, AdMob-swappable): FREE GOLD with a dual-gate cooldown + once-per-run defeat revive. Anime art restyle (31 assets incl. Beacon, title key art, walk-cycle sheets) + a procedural animation layer (walk bob/facing/squash/pops, tower recoil, projectile streaks; sheets auto-centered, misaligned ones dropped).
- **Post-launch — WC3 mechanics pass:** ✅ Damage-type × armor-type matchup matrix (pierce/siege/magic/poison/chaos vs unarmored/light/medium/heavy/fortified/boss) with UI badges derived from the table; Beacon aura tower (7th tower, +dmg/+atk-speed in radius, per-stat-max stacking, L4 Command/Haste fork); siege mode (sealing the maze is legal — besieged creeps follow a weighted breach field to the cheapest wall and destroy towers, no refund; orange seal warning in the build preview; save v2 carries tower HP). Rebalanced via `scratch/t10validate.mjs`: reference 6/6 seeds win (3–9 lives), all 3 heroes win, careless dies w20–28. Tests: t10matrix / t10aura / t10siege / t10save.
- **Phase 8 — Balance, save & juice:** ✅ Headless `sim/autoplay.js` (reference + careless strategies) used to retune the constants. Result: the reference build clears wave 100 on 6/6 seeds with 3–9 lives (low margin; threat concentrated in the final waves), all three heroes win, and careless dies ~w22–28. localStorage save/load (between waves, seed-rebuilt map — round-trip verified) + persistent high score + Continue option. Juice: screen shake on leaks/boss deaths + capped death particles. README how-to + balance-sim docs. Final game runs end-to-end headless (100 waves) and the HUD/render smoke-tests clean.
